classDiagram
    direction LR

    class User {
        - username: char* [PK]
        - password: char*
        - salt: char*
        + set_username(User*): void
        + set_password(User*): void
        + display_user(User*): void
        + save_user(User*): void
        + free_user(User*): void
    }

    class Product {
        - id: unsigned long long [PK]
        - name: char*
        - description: char*
        - username: char* [FK]
        - unit_price: float
        - quantity: size_t
        - alert_threshold: size_t
        - last_entry_date: Date
        - last_exit_date: Date
        + set_name(Product*): void
        + set_description(Product*): void
        + set_unit_price(Product*): void
        + set_quantity(Product*): void
        + set_alert_threshold(Product*): void
        + display_product(Product*): void
        + save_product(Product*): void
        + free_product(Product*): void
    }

    class Date {
        + day: uint8_t
        + month: uint8_t
        + year: uint16_t
    }

    User "1" -- "*" Product : manages
    Product "*" -- "2" Date : has